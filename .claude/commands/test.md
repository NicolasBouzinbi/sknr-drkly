Run the full test suite with `uv run pytest -v --tb=short`.
If any tests fail, analyze the failures and fix them.
Then run `uv run ruff check .` and fix any linting issues.
Report a summary of results: tests passed/failed, lint issues found/fixed.
